// Clasificacion Recurrentes
let
    Origen = Sql.Database("anl-meccano-sql.database.windows.net", "anlmeccanodw", [Query="SET DATEFIRST 1 -- ( Monday )#(lf)#(lf)--------------------------------------------------------------------------------------------------------------------------------#(lf)declare @year int#(lf)declare @yearA1 int#(lf)declare @yearA2 int#(lf)declare @FechaIni nvarchar(60), @FechaFin  nvarchar(60)#(lf)declare @FechaIniA1 nvarchar(60), @FechaFinA1  nvarchar(60)#(lf)declare @FechaIniA2 nvarchar(60), @FechaFinA2  nvarchar(60)#(lf)declare @EJFiscal int, @EJFiscalA1 int, @EJFiscalA2 int#(lf) #(lf)set @year = year(sysdatetime())#(lf)set @yearA1 = @year-1#(lf)set @yearA2 = @year-2#(lf)#(lf)SET @FechaIni = CONCAT(cast(@year as varchar(10)),'-02-01 00:00:00') #(lf)SET @FechaFin = CONCAT(cast(sysdatetime() as varchar(10)),' 23:59:59') #(lf)#(lf)select @EJFiscal = case when month(getdate()) > 1 then year(getdate()) else year(getdate())-1 end#(lf)set @EJFiscalA1 = @EJFiscal-1#(lf)set @EJFiscalA2 = @EJFiscal-2#(lf)#(lf)SET @FechaIniA1 = CONCAT(cast(@yearA1 as varchar(10)),'-02-01 00:00:00')#(lf)SET @FechaFinA1 = CONCAT(cast(@year as varchar(10)),'-01-31 23:59:59')#(lf)#(lf)#(lf)SET @FechaIniA2 = CONCAT(cast(@yearA2 as varchar(10)),'-02-01 00:00:00')#(lf)SET @FechaFinA2 = CONCAT(cast(@yearA1 as varchar(10)),'-01-31 23:59:59')#(lf)#(lf)--select @FechaIni,@FechaFin,@FechaIniA1,@FechaFinA1,@FechaIniA2,@FechaFinA2#(lf)#(lf)--------------------------------------------------------------------------------------------------------------------------------#(lf)/*with tabla_dates (ano, semana,lunes)#(lf)as (select ANO, SEMANA, min(FECHA_PEDIDO) as lunes#(lf)from (select distinct DATEPART(yy,FECHA_PEDIDO) AS ANO, DATEPART(wk,FECHA_PEDIDO) AS SEMANA, FECHA_PEDIDO#(lf)      from dbo_st.stg_ult_estado_linea_pedido_st #(lf)      where FECHA_PEDIDO >= '2018-01-01 00:00:00' and FECHA_PEDIDO <= '2022-02-02 00:00:00'#(lf)      and ID_CADENA in (6, 15) and ID_CENTRO_COMPRA in (2, 13, 16, 31)) a#(lf)group by ANO, SEMANA)#(lf)*/#(lf)--------------------------------------------------------------------------------------------------------------------------------#(lf)--FY ACTUAL#(lf)--------------------------------------------------------------------------------------------------------------------------------#(lf)select Pais.ID_PAIS, Pais.DESC_PAIS,#(lf)#(tab)#(tab)CLASIFICACION.EMAIL MAIL,#(lf)#(tab)#(tab)CLASIFICACION.N_PEDIDOS NUM_PEDIDOS,#(lf)#(tab)#(tab)CLASIFICACION.IMPORTE_BRUTO IMPORTE_BRUTO,#(lf)#(tab)#(tab)CLASIFICACION.IMPORTE_NETO IMPORTE_NETO,#(lf)#(tab)#(tab)CLASIFICACION.RanKing RANKING#(lf)from (select distinct ID_PAIS#(lf)      from dbo_st.stg_ult_estado_linea_pedido_st a11#(lf)#(tab)  join dbo_itx.STG_TIEMPO T on convert(char(10),a11.FECHA_PEDIDO) = convert(char(10),T.ID_FECHA)#(lf)      where FECHA_PEDIDO >= @FechaIni and FECHA_PEDIDO <= @FechaFin#(lf)      and ID_CADENA in (6, 15) and ID_CENTRO_COMPRA in (2, 13, 16, 31)#(lf)#(tab)  AND ID_PAIS in (1,2,38,4,5,6,3,30,11,25,21))#(tab)#(tab)BASE#(lf)--PAISES TOTALES#(lf)join (select 1 as ID_PAIS, 'FRANCIA' DESC_PAIS#(lf)      UNION#(lf)#(tab)  select 38 as ID_PAIS, 'MEXICO' DESC_PAIS#(lf)#(tab)  UNION#(lf)#(tab)  select 4 as ID_PAIS, 'ALEMANIA' DESC_PAIS#(lf)#(tab)  UNION#(lf)#(tab)  select 2 as ID_PAIS, 'BELGICA' DESC_PAIS#(lf)#(tab)  UNION#(lf)#(tab)  select 5 as ID_PAIS, 'ITALIA' DESC_PAIS#(lf)#(tab)  UNION#(lf)#(tab)  select 6 as ID_PAIS, 'UK' DESC_PAIS#(lf)#(tab)  UNION#(lf)#(tab)  select 3 as ID_PAIS, 'HOLANDA' DESC_PAIS#(lf)#(tab)  UNION#(lf)#(tab)  select 30 as ID_PAIS, 'RUSIA' DESC_PAIS#(lf)#(tab)  UNION#(lf)#(tab)  select 11 as ID_PAIS, 'ESPAÑA' DESC_PAIS#(lf)#(tab)  UNION#(lf)#(tab)  select 25 as ID_PAIS, 'POLONIA' DESC_PAIS#(lf)#(tab)  UNION#(lf)#(tab)  select 21 as ID_PAIS, 'TURQUIA' DESC_PAIS) Pais on (Base.ID_PAIS = Pais.ID_PAIS)#(lf)--PAISES A MOSTRAR#(lf)join (Select ID_PAIS,EMAIL,N_PEDIDOS,IMPORTE_NETO,IMPORTE_BRUTO,RanKing from (#(lf)select A.ID_PAIS as ID_PAIS,A.EMAIL as EMAIL,Sum(A.N_PEDIDOS) as N_PEDIDOS ,sum(IMPORTE_NETO) as IMPORTE_NETO,sum(IMPORTE_BRUTO) as IMPORTE_BRUTO,ROW_NUMBER() OVER(partition by A.ID_PAIS ORDER BY sum(A.IMPORTE_BRUTO) DESC) AS RanKing#(lf)#(tab)  from (select a.ID_PAIS, a.EMAIL,ID_FECHA, count(distinct a.ID_PEDIDO_ECOMMERCE) N_PEDIDOS,sum(IMPORTE_REAL*CAMBIO_REFERENCIA) as IMPORTE_NETO,sum(IMPORTE_PEDIDO*CAMBIO_REFERENCIA) as IMPORTE_BRUTO#(lf)#(tab)  from (select distinct a11.ID_PAIS, a12.EMAIL, a11.ID_PEDIDO_ECOMMERCE,a11.FECHA_PEDIDO,IMPORTE_REAL,IMPORTE_PEDIDO,CAMBIO_REFERENCIA#(lf)            from dbo_st.stg_ult_estado_linea_pedido_st#(tab)a11#(lf)            join dbo_st.stg_dim_usuario_ecomm_st#(tab)a12 on #(tab)a11.ID_USUARIO_ECOMM = a12.ID_USUARIO_ECOMM#(lf)#(tab)#(tab)#(tab)join dbo_itx.STG_MONEDA a13 on a11.ID_MONEDA = a13.ID_MONEDA#(lf)            where a11.FECHA_PEDIDO >=  @FechaIni and FECHA_PEDIDO <= @FechaFin#(lf)            and a11.ID_CADENA in (6, 15) and a11.ID_CENTRO_COMPRA in (2, 13, 16, 31)#(lf)#(tab)#(tab)#(tab)and ID_TIPO_DISPOSITIVO <> 5#(lf)            and a11.ID_PAIS in (1,2,38,4,5,6,3,30,11,25,21)#(lf)            and a11.FECHA_CANCELADO is null) a#(lf)#(tab)#(tab)#(tab)join dbo_itx.STG_TIEMPO T on cast(a.FECHA_PEDIDO as date) = cast(T.ID_FECHA as date)#(lf)#(tab)#(tab)#(tab)group by a.ID_PAIS, a.EMAIL,ID_EJERCICIO_FISCAL,ID_FECHA) A#(lf)#(tab) join (select distinct a11.ID_PAIS  ID_PAIS,#(tab)a12.EMAIL#(lf)            from#(tab)dbo_st.stg_ult_estado_linea_pedido_st#(tab)a11#(lf)      #(tab)    join#(tab)dbo_st.stg_dim_usuario_ecomm_st#(tab)a12#(tab)  on #(tab)a11.ID_USUARIO_ECOMM = a12.ID_USUARIO_ECOMM#(lf)            where#(tab)(a11.FECHA_PEDIDO >=  '2015-02-01 00:00:00' and a11.FECHA_PEDIDO < @FechaIni#(lf)            and a11.ID_CADENA in (6, 15) and a11.ID_CENTRO_COMPRA in (2, 13, 16, 31)#(lf)#(tab)#(tab)#(tab)and ID_TIPO_DISPOSITIVO <> 5#(lf)            and a11.ID_PAIS in (1,2,38,4,5,6,3,30,11,25,21)#(lf)            and a11.FECHA_CANCELADO is null)) B on (A.EMAIL = B.EMAIL and A.ID_PAIS = B.ID_PAIS)#(lf)#(tab) group by A.ID_PAIS,A.EMAIL#(lf)) TOT where TOT.Ranking <=10) CLASIFICACION on (BASE.ID_PAIS = CLASIFICACION.ID_PAIS)#(lf)--TOP 10 RECURRENTES IMPORTE BRUTO #(lf)", CreateNavigationProperties=false, CommandTimeout=#duration(0, 10, 0, 0)])
in
    Origen